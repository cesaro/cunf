
#include "cna/speccheck.hh"

namespace cna {

#if 0
void Speccheck::load_spec (const std::string & filename)
{
	FILE * f;

	if (spec) delete spec;
	spec = spec_parse (f, filename);
}
#endif

} // namespace cna

